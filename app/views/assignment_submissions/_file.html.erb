<pre><code class="prettyprint"><% file.body.split("\n").each_with_index do |line, index| %><small><%= index.to_s.ljust(3) %></small> <span class="line-of-code"> <%=line.ljust(80)%></span>
<% end %></code></pre>
