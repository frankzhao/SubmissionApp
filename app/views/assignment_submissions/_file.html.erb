<pre><code class="prettyprint"><% file.body.split("\n").each_with_index do |line, index| %><small><%= (index + 1).to_s.ljust(3) %></small> <span class="line-of-code"><%=line.rstrip%></span><br><% end %></code></pre>
